version: "2"
sql:
  - engine: "postgresql"
    queries: "internal/queries/"
    schema: "internal/migrations/"
    gen:
      go:
        package: "db"
        out: "internal/generated/db"
        emit_json_tags: true
        emit_interface: true
